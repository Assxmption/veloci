# ─── VELOCI Trend Intelligence Engine ─── Requirements ───
# Install: pip install -r requirements.txt
# Python >= 3.10 required
#
# After install, run:
#   python -m spacy download en_core_web_sm

# ── Scrapers ──────────────────────────────────────────────
praw>=7.7.0                   # Reddit API
google-api-python-client>=2.100.0  # YouTube Data API v3
tweepy>=4.14.0                # Twitter/X API
pytrends>=4.9.0               # Google Trends (unofficial)
feedparser>=6.0.0             # RSS feed parsing
httpx>=0.25.0                 # Instagram Apify + GDELT HTTP client
aiohttp>=3.9.0                # Async HTTP (Reddit, RSS)

# ── NLP & ML ─────────────────────────────────────────────
sentence-transformers>=2.5.0  # Semantic embeddings (all-MiniLM-L6-v2)
scikit-learn>=1.3.0           # DBSCAN clustering, TF-IDF
spacy>=3.7.0                  # Named entity recognition
numpy>=1.24.0                 # Numerical computing
scipy>=1.11.0                 # Scientific computing

# ── Storage ──────────────────────────────────────────────
aiosqlite>=0.19.0             # Async SQLite

# ── Orchestration ────────────────────────────────────────
APScheduler>=3.10.0           # Job scheduling for trend cycles
python-dotenv>=1.0.0          # .env file support

# ── Utilities ────────────────────────────────────────────
loguru>=0.7.0                 # Structured logging
